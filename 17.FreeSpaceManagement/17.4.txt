Best:
排序不影响内存分配时目标块的命中时间，应为他会始终遍历整个列表寻找最适合的块

WORST:
排序不影响内存分配时目标块的命中时间，应为他会始终遍历整个列表寻找最大的块

First:
当排序为SIZESORT-时，在分配内存时他的目标块匹配速度会最快
当排序为SIZESORT+时，在空间利用率方面它会达到Best匹配的效果


对于三种匹配策略，当排序为ADDRSORT时都会加快空闲块合并的速度